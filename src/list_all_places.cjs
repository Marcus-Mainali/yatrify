const { ADVENTURE_PLACES } = require('./src/data/adventure.ts');
const { NATURE_PLACES } = require('./src/data/nature.ts');
const { CULTURAL_PLACES } = require('./src/data/cultural.ts');

// Wait! Since these are TypeScript files, we need to run them, but we can also just read them with a simple JS script using regex, or parse them.
// Let's write a simple JS regex parser to extract all places!
